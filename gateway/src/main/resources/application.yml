eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/
  instance:
    prefer-ip-address: true
server:
  port: 8001
spring:
  application:
    name: service-ribbon
zuul:
  routes:
    service-feign:
      path: /service-feign/**
      serviceId: service-feign
